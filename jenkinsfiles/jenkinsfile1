pipeline {
    agent {
        label 'ubuntu_docker'
    }
    stage("create docker image") {
            steps {
                echo " ============== start building image =================="
                dir ('docker/images') {
                	sh 'docker build . '
                }
            }
        }
    }
