pipeline {
    agent {
        label 'ubuntu_docker'
    }
    stages {
        stage("create docker image") {
            steps {
                echo " ============== add index.html =================="
                    sh 'touch indedx.html'
                    sh 'echo dddddddddddtest>>index.html'
            }
         }        
     }
 }
