pipeline {
    agent {
        label 'ubuntu_docker'
    }
    stages {
        stage('Build') {
            agent {
                docker {
                    image 'hello-world'
                    // Run the container on the node specified at the top-level of the Pipeline, in the same workspace, rather than on a new node entirely:
                    reuseNode true
                }
            }
            steps {
                sh 'hello-world --version'
            }
        }
    }
}
